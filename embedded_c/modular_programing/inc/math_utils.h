/*File: math_utils.h*/ 

// Dòng này gọi là "Include Guard" (Bảo vệ chống nhúng lặp file)
#ifndef MATH_UTILS_H
#define MATH_UTILS_H

/*Khai báo tên hàm (Chỉ có tên, tham số, kiểu trả về, kết thúc bằng dấu chấm phẩy)*/
int cong(int a, int b);
int tru(int a, int b);

#endif